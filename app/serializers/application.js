import DS from 'ember-data';

export default DS.JSONAPISerializer.extend({

  // payloadKeyFromModelName(modelName) {
  //   return singularize(capitalize(modelName));
  // }
  // keyForAttribute(attr) { return underscore(attr); }

});
